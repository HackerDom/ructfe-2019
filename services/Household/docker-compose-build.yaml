version: '2.2'
services:
    household_build:
        container_name: household-build
        build:
            context: .
            dockerfile: Dockerfile-build
        volumes:
            - ./publish:/app/Household/publish